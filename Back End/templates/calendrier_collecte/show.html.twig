{% extends 'base.html.twig' %}

{% block title %}CalendrierCollecte{% endblock %}

{% block body %}
    <h1>CalendrierCollecte</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ calendrier_collecte.id }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ calendrier_collecte.adresse }}</td>
            </tr>
            <tr>
                <th>TypeDechet</th>
                <td>{{ calendrier_collecte.typeDechet }}</td>
            </tr>
            <tr>
                <th>JourCollecte</th>
                <td>{{ calendrier_collecte.jourCollecte ? calendrier_collecte.jourCollecte|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Remarque</th>
                <td>{{ calendrier_collecte.remarque }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_calendrier_collecte_index') }}">back to list</a>

    <a href="{{ path('app_calendrier_collecte_edit', {'id': calendrier_collecte.id}) }}">edit</a>

    {{ include('calendrier_collecte/_delete_form.html.twig') }}
{% endblock %}
