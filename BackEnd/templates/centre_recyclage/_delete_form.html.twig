<form method="post" action="{{ path('app_centre_recyclage_delete', {'id': centre_recyclage.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ centre_recyclage.id) }}">
    <button class="btn">Delete</button>
</form>
