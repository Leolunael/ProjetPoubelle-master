<!-- ✅ Barre de navigation -->

<nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-4">
    <div class="container">
        <a class="navbar-brand" href="{{ path('app_home') }}">DéchetsApp</a>

        <div>
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item"><a class="nav-link" href="{{ path('signalement_new') }}">Signaler</a></li>
                <li class="nav-item"><a class="nav-link" href="{{ path('calendrier_index') }}">Calendrier de collecte</a></li>
                <li class="nav-item"><a class="nav-link" href="{{ path('centre_liste') }}">Centres de recyclage</a></li>
                <li class="nav-item"><a class="nav-link" href="{{ path('centre_new') }}">Renseigner un centre</a></li>
                <li class="nav-item"><a class="nav-link" href="{{ path('enfant_index') }}">Enfants</a></li>

                {% if app.user %}
                   <li class="nav-item">
                   <a class="nav-link" href="{{ path('user_points') }}">Espace utilisateur</a>
                   </li>


                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_logout') }}"
                           onclick="event.preventDefault(); document.getElementById('logout-form').submit();">
                            Déconnexion
                        </a>
                        <form id="logout-form" action="{{ path('app_logout') }}" method="POST" style="display: none;"></form>
                    </li>
                {% else %}
                    <li class="nav-item"><a class="nav-link" href="{{ path('app_login') }}">Connexion</a></li>
                    <li class="nav-item"><a class="nav-link" href="{{ path('app_register') }}">Inscription</a></li>
                {% endif %}

                {% if is_granted('ROLE_ADMIN') %}
                    <li class="nav-item"><a class="nav-link" href="{{ path('admin_dashboard') }}">Admin</a></li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>
